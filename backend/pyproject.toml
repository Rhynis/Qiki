[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "B", "A", "C4", "UP", "RUF", "S", "BLE", "ASYNC"]
ignore = ["S101", "S104", "N818"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101", "S105", "S106"]
# Data migrations embed verbatim Vietnamese product copy, which legitimately
# contains long lines (E501) and typographic dashes ruff flags as ambiguous
# (RUF001); the text must be preserved exactly, so allow both here.
"migrations/*" = ["E501", "RUF001"]

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
warn_unused_ignores = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = [
  "app.main",
  "app.core.security_middleware",
  "app.db.redis",
  "app.api.v1.endpoints.auth",
  "app.api.v1.endpoints.products",
  "app.api.v1.endpoints.orders",
  "app.api.v1.endpoints.coupons",
  "app.api.v1.endpoints.deliveries",
  "app.api.v1.endpoints.admin_users",
  "app.api.v1.endpoints.admin_chat",
  "app.api.v1.endpoints.admin_dashboard",
  "app.api.v1.endpoints.admin_insights",
  "app.api.v1.endpoints.knowledge_base",
  "app.api.v1.endpoints.rag",
  "app.api.v1.endpoints.conversations",
  "app.api.v1.endpoints.review",
  "app.api.v1.endpoints.price_alerts",
  "app.api.v1.endpoints.wishlist",
  "app.api.v1.endpoints.agent",
]
disable_error_code = ["type-arg"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.coverage.run]
source = ["app"]
omit = ["tests/*", "migrations/*"]

[tool.coverage.report]
exclude_lines = [
  "pragma: no cover",
  "raise NotImplementedError",
  "if TYPE_CHECKING:",
]
