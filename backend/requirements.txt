fastapi>=0.136.3,<1.0.0
starlette>=1.0.1,<2.0.0
# Bumped 0.27.0->0.35.0 for issue #349: fastmcp==3.4.7's [server] extra
# requires uvicorn>=0.35 (mcp==1.29.0, fastmcp's SDK dep, only needs >=0.31.1).
uvicorn[standard]==0.35.0
pydantic>=2.12.0,<3.0.0
pydantic-settings>=2.12.0,<3.0.0
sqlalchemy[asyncio]==2.0.27
asyncpg==0.29.0
alembic==1.13.0
PyJWT[crypto]>=2.10.1,<3.0.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart>=0.0.27,<1.0.0
httpx>=0.28.0,<0.29.0
google-genai>=2.7.0,<3.0.0
jinja2==3.1.6
# Bumped 2.20.0->2.46.0 (still 2.x, no API changes used by app/llm/observability.py)
# for issue #349: langfuse<2.46.0 pins packaging<24.0, which conflicts with the new
# fastmcp==3.4.7 dependency below (packaging>=24.0). 2.46.0 is the earliest 2.x
# release that relaxes it (packaging<25.0,>=24.1).
langfuse==2.46.0
numpy>=1.24.0
rank-bm25==0.2.2
structlog==24.1.0
slowapi==0.1.9
redis==5.0.1
tenacity==9.1.4
underthesea==6.8.0
sentry-sdk[fastapi]>=2.8.0,<3.0.0
email-validator==2.1.0
aiosmtplib==5.1.1
pgvector==0.2.5
# Agent orchestration (feature-flagged, see docs/adr/0001-langgraph-agent-orchestration.md).
# langgraph-checkpoint-postgres talks to Postgres via psycopg (v3), separate from
# the asyncpg/SQLAlchemy pool the rest of the app uses for business tables.
langgraph==1.2.11
langgraph-checkpoint-postgres==3.1.2
psycopg[binary,pool]==3.3.4
# MCP server (feature-flagged behind MCP_ENABLED, see issue #349 / docs/mcp.md)
# exposing the agent's read-only tools over Streamable HTTP. `mcp` (the
# protocol SDK) is a transitive dep of fastmcp but pinned explicitly too, same
# as langgraph-checkpoint-postgres above.
fastmcp==3.4.7
mcp==1.29.0
