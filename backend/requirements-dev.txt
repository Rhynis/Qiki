pytest>=9.0.3,<10.0.0
pytest-asyncio>=1.3.0,<2.0.0
pytest-cov==4.1.0
pytest-mock==3.12.0
httpx>=0.28.0,<0.29.0
faker==23.0.0
fakeredis==2.21.0
pytest-httpx>=0.36.0,<1.0.0
ruff==0.3.0
mypy==1.8.0
pip-audit==2.7.0
types-passlib
# Dev/test only — conversation eval suite (tests/eval). Kept out of the runtime image.
deepeval==4.0.6
# NOTE: deepeval's transitive build tool `wheel` is held at 0.45.1 (GHSA-8rrh-rw8j-w5fx)
# because the fix (wheel 0.46.2+) requires packaging>=24, which the runtime `langfuse`
# pins to <24. wheel is build-only and not shipped in the runtime image, so pip-audit
# ignores that single advisory (see .github/workflows/ci-backend.yml).
# Client for deepeval's OllamaModel (app/evaluation/agent_evaluator.py's LLM-judge
# subset) — lazily imported by deepeval only when a judge call actually runs, so this
# is a soft requirement in practice, but must be installed for that path to work.
ollama==0.6.2
